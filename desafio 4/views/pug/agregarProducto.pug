extends layout/main

block content
  main(class="container-lg")
    section(class="row justify-content-around")
      if respuesta
        h1(class="col-12 text-center mb-4") Producto Agregado
        article(class="col-8 col-md-6 col-lg-3 d-flex flex-column p-2 text-center card border-success")
          img(class="img-fluid rounded" src=respuesta.imagen, alt=respuesta.nombre)
          h3(class="text-sencondary")=respuesta.nombre
          b="$ " + respuesta.precio
      else 
        h4(class="text-center") Alguno de los campos ha quedado sin rellenar!
    form(action="/" method="get")
    button(class="btn btn-danger" type="submit") Volver
